import { ReadShape, Schema } from "../../resource";
declare type UseErrorReturn<P> = P extends null ? undefined : Error;
/** Access a resource or error if failed to get it */
export default function useError<Params extends Readonly<object>, S extends Schema>(fetchShape: ReadShape<S, Params>, params: Params | null, cacheReady: boolean): UseErrorReturn<typeof params>;
export {};
