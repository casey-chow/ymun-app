import { ReadShape, Schema } from "../../resource";
/** Access a resource if it is available. */
export default function useCache<Params extends Readonly<object>, S extends Schema>(fetchShape: Pick<ReadShape<S, Params>, 'schema' | 'getFetchKey'>, params: Params | null): import("../../resource").DenormalizeNullable<S>;
