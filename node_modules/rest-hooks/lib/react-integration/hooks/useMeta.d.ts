import { FetchShape } from "../../resource";
/** Gets meta for a url. */
export default function useMeta<Params extends Readonly<object>>({ getFetchKey }: Pick<FetchShape<any, any, Params>, 'getFetchKey'>, params: Params | null): {
    date: number;
    error?: Error | undefined;
    expiresAt: number;
} | null;
