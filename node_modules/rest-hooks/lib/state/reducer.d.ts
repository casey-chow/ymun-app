import { ActionTypes, State } from "../types";
export declare const initialState: State<unknown>;
export default function reducer(state: State<unknown> | undefined, action: ActionTypes): State<unknown>;
