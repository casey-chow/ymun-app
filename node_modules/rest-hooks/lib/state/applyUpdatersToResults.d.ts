import { UpdateFunction } from "../types";
import { Normalize, Schema } from "../resource";
declare type ResultStateFromUpdateFunctions<SourceSchema extends Schema, UpdateFunctions extends {
    [key: string]: UpdateFunction<SourceSchema, any>;
}> = {
    [K in keyof UpdateFunctions]: any;
};
export default function applyUpdatersToResults<SourceSchema extends Schema, UpdateFunctions extends {
    [key: string]: UpdateFunction<SourceSchema, any>;
}>(results: ResultStateFromUpdateFunctions<SourceSchema, UpdateFunctions>, result: Normalize<SourceSchema>, updaters: UpdateFunctions | undefined): ResultStateFromUpdateFunctions<SourceSchema, UpdateFunctions>;
export {};
