import { NetworkManager, FetchAction, ReceiveAction, Dispatch } from '..';
export declare class MockNetworkManager extends NetworkManager {
    handleFetch(action: FetchAction, dispatch: Dispatch<any>): Promise<any>;
    handleReceive(action: ReceiveAction): void;
}
