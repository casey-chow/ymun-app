import React from 'react';
import { IonList, IonItem, IonLabel } from '@ionic/react';

export const Maps: React.FC = () => (
  <IonList>
    <IonItem>
      <IonLabel>Hershey Lodge</IonLabel>
    </IonItem>
    <IonItem>
      <IonLabel>Hershey Lodge (Delegation Meetings)</IonLabel>
    </IonItem>
  </IonList>
);

export default Maps;
